{% extends 'base.html' %}
{% block content %}
<body class="section light-bg" id="confirmation">
   <div class="container">
       <div class="section-title">
            <h2>Confirmed!</h2>
            <h3>{{building}} - {{room}}</h3>
            <h2>{{fullTime}}</h2>
       </div>
</body>
 {% endblock %}
